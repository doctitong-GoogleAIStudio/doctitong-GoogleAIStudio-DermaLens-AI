# DermaLens AI backend — runtime dependencies.
#
# Trimmed for self-hosting: the Emergent-only packages are gone —
# `emergentintegrations` and the `litellm` wheel that was served from
# customer-assets.emergentagent.com (a URL that only resolves inside Emergent's
# build environment, so `pip install -r requirements.txt` used to fail anywhere
# else). Neither was ever imported: Gemini is called directly over HTTPS from
# server.py with httpx.
#
# Versions match the ones the app was last verified on. Dev/test-only packages
# live in requirements-dev.txt.

# --- Web framework -----------------------------------------------------------
fastapi==0.110.1
starlette==0.37.2
uvicorn[standard]==0.25.0
pydantic==2.13.5
email-validator==2.3.0
python-multipart==0.0.32

# --- Database ----------------------------------------------------------------
motor==3.3.1
pymongo==4.6.3
dnspython==2.8.0          # required for mongodb+srv:// connection strings

# --- Auth --------------------------------------------------------------------
passlib==1.7.4
bcrypt==4.1.3
PyJWT==2.13.0

# --- HTTP client (Gemini, Google Play, Resend) -------------------------------
httpx==0.28.1
requests==2.34.2          # google.auth.transport.requests needs this

# --- Google Play subscription verification (optional at runtime) -------------
google-auth==2.57.1

# --- PDF report rendering ----------------------------------------------------
xhtml2pdf==0.2.18
reportlab==5.0.1
svglib==2.2.0
html5lib==1.1
pypdf==6.17.0
pillow==12.3.0

# --- Markdown (privacy policy page) ------------------------------------------
markdown-it-py==4.2.0

# --- Config ------------------------------------------------------------------
python-dotenv==1.2.3
